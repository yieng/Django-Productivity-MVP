# Django-Productivity-MVP
I built a custom task-tracking dashboard in Django to manage my financial goals. I handled the data modeling using Django’s ORM, implemented custom business logic for dynamic credit scoring, and integrated Bootstrap for a responsive UI. I also managed the full deployment lifecycle, including database migrations.
